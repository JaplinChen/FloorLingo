[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "floorlingo"
version = "0.1.0"
description = "Official Python SDK for FloorLingo WhatsApp API Gateway"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
authors = [
    { name = "FloorLingo Contributors" }
]
keywords = ["whatsapp", "api", "sdk", "floorlingo", "gateway"]

dependencies = [
    "httpx>=0.25.0,<1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.23",
]

[project.urls]
Homepage = "https://github.com/JaplinChen/FloorLingo"
Repository = "https://github.com/JaplinChen/FloorLingo"

[tool.setuptools.packages.find]
include = ["floorlingo*"]

# Ship type information (PEP 561) so consumers get the bundled type hints.
[tool.setuptools.package-data]
floorlingo = ["py.typed"]
"floorlingo.resources" = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
